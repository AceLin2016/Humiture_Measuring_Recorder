

// loop() function 的 UML 图：
/*      设定时间？ ->设定时间前什么都不做 
 *          | 设定完毕
 *      创建循环任务（在更改时间后才有次处理）
 *          |
 *      判断是否做读取数据并处理工作 -> 否 -do nothing
 *          | 是
 *          读取DHT11 的数据 --读取数据失败--> 继续下面的操作，但是对数据赋予错误警示值
 *              | 读取数据成功
 *          读取时间数据  --读取时间失败--> （罕见错误）指示灯提醒
 *              | 读取时间成功
 *  将       是否创建新文件（新的一天？）（或着本段称作打开哪个文件？）
 *  数           |  
 *  据       打开文件 （如果是新的一天的文件，不存在，但打开将直接创立）--打开文件失败--> 罕见错误，指示灯提示
 *  写           | 打开成功
 *  入       该文件是否是新文件？（如果是新文件，在首行需要添加“键”）
 *              |
 *          写入数据 （以CSV格式写入 ）
 *              | 写入数据完
 *          关闭文件 --失败--> 罕见错误
 *    调用Alarm.delay(0);
*/